{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and recording popup",
  "windows": ["main", "recording-popup"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "opener:default",
    "updater:default"
  ]
}
