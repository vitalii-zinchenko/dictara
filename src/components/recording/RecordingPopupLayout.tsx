import type { ReactNode } from 'react'

interface RecordingPopupLayoutProps {
  children: ReactNode
}

export function RecordingPopupLayout({ children }: RecordingPopupLayoutProps) {
  return <>{children}</>
}
